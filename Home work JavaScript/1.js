/* 1. Дано змінні a і b. Перевірте, що "a" ділиться без залишку на "b". 
Якщо це так - виведіть 'Ділиться' і результат поділу, 
інакше виведіть 'Ділиться з залишком' та залишок від поділу.
*/
var a = 5
var b = 3
var c = a / b
if (Number.isInteger(c)) {
    let x = ('Ділиться ' + c)
console.log(x)
} else { 
    var u = a%b
    let y = ('Ділиться з залишком ' + u)
    console.log(y)
}
